#!/bin/bash

if [ ! -f "Makefile" ] || [ ! -f "docker-compose.yml" ]; then
    echo "Makefile or docker-compose.yml not found. Is this a compose directory?"
fi

cp $HOME/.bash_aliases etc/.bash_aliases
